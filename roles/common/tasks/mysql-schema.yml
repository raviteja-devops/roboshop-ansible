- name: Copy Mongodb Yum Repo File
  ansible.builtin.copy:
    src: mysql.repo
    dest: /etc/yum.repos.d/mysql.repo

- name: Install Mongodb
  ansible.builtin.yum:
    name: mysql-client
    state: installed

- name: Load Schema
  community.mongodb.mongodb_shell:
    login_host: mongodb-dev.raviteja.online
    file: "/app/schema/{{component}}.js"
    idempotent: yes
